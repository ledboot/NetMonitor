apply plugin: 'groovy'

dependencies {
    compile gradleApi()
    compile localGroovy()
    compile 'commons-io:commons-io:2.4'
    compile 'org.ow2.asm:asm:6.0_ALPHA'
}

repositories {
    jcenter()
}
